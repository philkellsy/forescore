<nav class="navbar navbar-expand-lg navbar-dark legends-nav">
  <div class="container">
    <a class="navbar-brand fw-bold" href="/">Legends</a>
    <% if (user) { %>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navMenu">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navMenu">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item"><a class="nav-link" href="/player/dashboard">Player</a></li>
          <li class="nav-item"><a class="nav-link" href="/scoring">Scoring</a></li>
          <li class="nav-item"><a class="nav-link" href="/leaderboards">Leaderboards</a></li>
          <% if (user.role === 'admin') { %>
            <li class="nav-item"><a class="nav-link" href="/admin/dashboard">Admin</a></li>
          <% } %>
        </ul>
        <div class="d-flex align-items-center gap-3">
          <small class="text-light"><%= user.firstName %> <%= user.lastName %></small>
          <form action="/auth/logout" method="post" class="m-0">
            <button class="btn btn-sm btn-outline-light" type="submit">Logout</button>
          </form>
        </div>
      </div>
    <% } %>
  </div>
</nav>
